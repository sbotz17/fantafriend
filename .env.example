# Usato da drizzle-kit (generate/migrate/studio) tramite dotenv.
# Copia questo file in `.env` e inserisci la tua connection string Neon.
DATABASE_URL="postgres://utente:password@host.neon.tech/db?sslmode=require"
